jquery-scrollEl
===============

window position scrollto this or top or specify position

  Usage:
  
    $(".scrollToThis").scrollEl();
 
    $(".scrollToThis").scrollEl({
 			offsetTop:40
    });

    $(".scrollToTop").scrollEl({
 			offsetTop:40,
 			mode:'toTop'
    });
